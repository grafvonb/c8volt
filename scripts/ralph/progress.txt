## Codebase Patterns
- `cmd` completion regressions should execute Cobra's hidden `__complete` or `__completeNoDesc` root entry points after resetting shared flags; this exercises the real completion seam without needing shell wrappers.

# Ralph Progress Log
Started: Tue Mar 24 16:33:34 CET 2026
---
## [2026-03-24 16:35:50 CET] - #82-001
Codex output: no `scripts/ralph/.codex-last-message-iter-*.txt` file was present for this iteration
- Implemented an explicit shared completion seam in `cmd/cmd_checks.go` and routed root bootstrap bypass decisions through it from `cmd/root.go`.
- Added reusable completion execution helpers and foundational regression coverage proving hidden completion entry points bypass config/bootstrap noise and stay rooted in the existing Cobra command tree.
- Added a repo-level testing note in `AGENTS.md` for future completion regression work.
- Files changed: `AGENTS.md`, `cmd/cmd_checks.go`, `cmd/get_test.go`, `cmd/mutation_test.go`, `cmd/root.go`, `scripts/ralph/prd.json`, `scripts/ralph/progress.txt`
- **Learnings for future iterations:**
  - The stable repository-native completion seam is the root bootstrap decision in `cmd/root.go` plus command classification in `cmd/cmd_checks.go`.
  - Completion regressions can run in-process by executing `Root()` with `__complete` arguments and the shared flag-reset helper; shell-specific installation is not needed for foundational coverage.
  - Hidden Cobra completion commands should bypass config/bootstrap validation the same way user-facing utility commands do, or completion tests may pick up config/help noise.
---
