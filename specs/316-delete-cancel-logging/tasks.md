# Tasks: Delete and Cancel Logging Consolidation

**Branch**: `codex/316-delete-cancel-logging`
**Input**: [spec.md](spec.md), [plan.md](plan.md), [research.md](research.md), [contract](contracts/cli-logging.md).

Seven cohesive work units. Tests belong to the fix they verify. Read the feature context and repository instructions before implementation; this is normal preparation, not a separate deliverable. No new dependencies, flags, result schemas, or logging framework.

- [ ] T001 [US1] Reproduce the reported failure in `cmd/processinstance_logging_transcript_test.go`: use existing local HTTP/config/subprocess helpers for child deletion 409 → root cancellation 204 → ACTIVE observations → short confirmation timeout. Capture stdout/stderr separately and assert existing request order, error class and machine result shape. Add failing assertions for the required concise human transcript; keep fixture helpers in this test file unless another test actually needs them.
- [ ] T002 [US3] Consolidate polling in `internal/services/processinstance/waiter/waiter.go`: one completed observation per actual check, including key, attempt, state or lookup failure, elapsed time and conditional next delay. Add one narrow nested-lookup suppression option in `internal/services/calloption.go` only if existing options cannot express it; apply it consistently in `internal/services/processinstance/v87/service.go`, `internal/services/processinstance/v88/service.go`, `internal/services/processinstance/v89/service.go`, and `internal/services/processinstance/v810/service.go`. Keep direct lookup diagnostics, v87 tenant-safe search, existing waits/timers/requests and activity behavior. Extend `internal/services/processinstance/waiter/waiter_test.go` and the affected adapter tests for success, absence, failed lookup, stop/timeout and scoped suppression; retain last observed state locally for T005.
- [ ] T003 [US3] Remove routine OAuth cache lookup/hit DEBUG messages in `internal/services/auth/oauth2/service.go`; keep acquisition, refresh, failure and redaction behavior. Extend `internal/services/auth/oauth2/diagnostics_test.go` to prove cache reuse and unchanged exchanges. Add `TestProcessInstancePollingRecordBudget` in `cmd/processinstance_logging_transcript_test.go` using a deterministic 36-check cached-auth fixture; require 36 observations plus 36 unchanged HTTP records, excluding bootstrap and phase records.
- [ ] T004 [US2] Restore only the required verbose explanations at existing delete/cancel transition sites in `internal/services/processinstance/v87/service.go`, `internal/services/processinstance/v88/service.go`, `internal/services/processinstance/v89/service.go`, and `internal/services/processinstance/v810/service.go`, using existing verbose logging helpers in `internal/services/common/logging.go`. Selectively adjust suppression and effective verbose options in `cmd/processinstance_mutation_progress.go` so cancellation prerequisite, root escalation, accepted submission, wait scope/states/timeout/backoff, and actual resumption each have one owner. Keep bulk/per-key lifecycle suppression, existing progress callbacks, and both real confirmation waits. Extend `cmd/processinstance_logging_transcript_test.go` for verbose-only, DEBUG-only, both, successful resumption and timeout; preserve quiet/automation/machine-mode guards. Do not add a workflow event kind or public callback payload.
- [ ] T005 [US1] Make failure presentation concise using existing completion `FailureDetail` in `internal/services/processinstance/bulk.go`, existing command progress routing in `cmd/processinstance_mutation_progress.go`, and the final error boundary in `cmd/cmd_views_contract.go`. Carry only needed last-state and mutation facts from the waiter/adapter failure branches, using a small error wrapper if required; preserve full Error() text and wrapped/joined causes, with the narrow correction in `c8volt/ferrors/errors.go` needed to keep them inspectable. Put human formatting in `cmd/cmd_views_processinstance_logging.go`. Show failed phase/root/scope/timeout/available last states, accepted cancellation and unreached deletion; never claim rollback or definitive cancellation failure. Log the full chain once at DEBUG at the final boundary and preserve original JSON detail/class/exit behavior. Extend `c8volt/ferrors/errors_test.go` and the transcript test for cause inspection, normalization/class precedence, unknown/partial states and multiple roots. No general stage ledger, projection DTO or completion payload extension.
- [ ] T006 [US4] Complete the combined output compatibility cases in `cmd/processinstance_logging_transcript_test.go` and reuse existing cases in `cmd/cmd_confirmation_terminal_test.go`, `cmd/delete_processinstance_selector_test.go`, and `cmd/cancel_processinstance_selector_test.go`. Verify one JSON value then EOF, exact keys output/zero-byte empty keys, configured/inherited stderr with real terminal stdin, quiet and automation combinations, dry-run/no-wait, empty/sparse scopes and aborts. Assert request order/counts and exit classification; retain tenant and HTTP diagnostic contracts. Run affected facade, all-adapter and `internal/services/processdefinition/delete_test.go` regressions, adding cases only where existing coverage does not prove the changed path.
- [ ] T007 Update logging guidance in `README.md` and affected metadata in `cmd/delete_processinstance.go` and `cmd/cancel_processinstance.go`; run `make docs-content`, gofmt on touched Go files, `git diff --check`, targeted checks and `make test`. Record actual validation results and terminal skips/blockers in `specs/316-delete-cancel-logging/quickstart.md`. Do not mark the issue complete with failing required checks.

## Execution and Validation

Execute T001 → T002 → T003 → T004 → T005 → T006 → T007. T001 is the failing reproduction and becomes green as fixes land; keep tests and implementation in a validated logical commit. T003's OAuth-only change can be prepared independently, but its record-budget assertion depends on T002. Otherwise work sequentially because the tasks share files. No parallel-agent program is needed.

| Story | Tasks | Independent acceptance |
| --- | --- | --- |
| US1: timeout clarity | T001, T005 | Normal transcript explains the unconfirmed cancellation; original causes and machine errors survive. |
| US2: verbose progress | T004 | Each actual transition is explained once; verbose and DEBUG remain independent. |
| US3: polling diagnostics | T002, T003 | 36 cached-auth checks produce 72 polling records; direct lookup evidence remains. |
| US4: compatibility | T006 | Results, requests, prompts and automation behavior remain unchanged. |
| Delivery | T007 | Documentation matches behavior and the required suite passes. |

The smallest useful demonstration is T001 plus the timeout-summary portion of T005. Deliver this focused issue as one cohesive fix unless a tested split proves useful. Follow Conventional Commits and the constitution's full-test requirement before implementation commits; no separate setup, model, mapping, or paperwork commits are required.
