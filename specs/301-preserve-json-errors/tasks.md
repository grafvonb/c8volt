---
description: "Implementation tasks for issue #301: preserve existing JSON error envelopes"
---

# Tasks: Preserve JSON Error Envelopes

**Input**: Design documents from `specs/301-preserve-json-errors/`

**Prerequisites**: [plan.md](plan.md), [spec.md](spec.md), [research.md](research.md), [data-model.md](data-model.md), [CLI contract](contracts/cli-error-envelope.md), [quickstart.md](quickstart.md), and `.specify/memory/constitution.md`.

**Tests**: Required explicitly by FR-010 and FR-011. Use command execution and separately captured streams; renderer-only tests are insufficient. Write regression cases before the associated dispatch change and demonstrate the expected failure, then make them pass. Existing compatibility cases need not fail first.

**Organization**: Three story increments, followed by documentation and final validation. All tasks begin unchecked; task generation does not implement them.

## Format and Path Conventions

Task lines use `- [ ] Tnnn [P?] [USn?] Description`. Paths are relative to the repository root. `[P]` marks work in separate files that can proceed concurrently once its prerequisites are complete; it does not authorize parallel Go tests that mutate package globals. Reuse `cmd/`, `testx/`, and existing error policy; no new dependencies or schema are required.

## Phase 1: Setup

**Purpose**: Confirm the bounded correction and prepare a repeatable local validation environment.

- [x] T001 Confirm branch `codex/301-preserve-json-errors`, active feature resolution in `.specify/feature.json`, and the direct-exit/caller inventory in `specs/301-preserve-json-errors/research.md`; inspect `cmd/` for drift and retain the documented bootstrap, non-full, raw-XML, and unreachable-renderer exclusions.
- [x] T002 Verify the Go/toolchain requirements in `go.mod` and available subprocess/config/HTTP fixtures in `testx/cmd_subprocess_runner.go`, `testx/cmd_config.go`, and `cmd/get_test.go`; run the existing focused regression command from `specs/301-preserve-json-errors/quickstart.md` and record baseline results there without changing production dependencies.

## Phase 2: Foundational Test Support

**Purpose**: Establish common assertions before any story tests. Production error classification and rendering already exist and must be reused.

- [x] T003 Add command-test-only shared support in `cmd/cmd_error_envelope_test.go`, reusing `testx.RunCmdSubprocessInDirWithSeparateOutputs` and exact helper-process scoping, for isolated configuration, separate stdout/stderr, expected process status, and one JSON decode followed by EOF; assert data-model constraints verbatim: outcome is “`invalid` for invalid input; otherwise existing `failed` outcome”, envelope class is “Existing normalized classification”, command is “Canonical command path from the actual Cobra command”, tenantContext is “Attached by the existing renderer only when already available”, payload is “Omitted for these errors; do not add empty operation reports”, detail is “Present for these errors with normalized message and class”, detail.message is “Trimmed normalized error text; preserve contextual meaning and corrective guidance”, detail.class is “Same existing classification as envelope class”, and suggestion is “Existing behavior leaves it absent on this path; do not move guidance into a new field”.

**Checkpoint**: Reusable assertion support exists; no production schema, policy, renderer, or contract annotation changes. Tests compare independent expected values, not expectations generated by the same production error renderer.

## Phase 3: User Story 1 — Actionable Validation Errors (Priority: P1, MVP)

**Goal**: Invalid deletion search options and invalid piped keys produce the promised structured error.

**Independent Test**: Exercise deletion search validation and all 12 stdin callers with JSON/human output and exit-code suppression on/off. JSON must decode once then reach EOF; human stdout is empty; diagnostics, classification, command identity, and termination remain correct.

### Tests

- [x] T004 [P] [US1] Add `TestCommandErrorEnvelopeDeleteValidation` subprocess coverage in `cmd/delete_processinstance_error_envelope_test.go` for invalid search options that pass argument parsing but fail `validatePISearchFlags`; cover JSON/human × default/`--no-err-codes`, assert `invalid`/`invalid_input`, precise existing message and exit status, and zero discovery/mutation or subsequent work after rejection.
- [x] T005 [P] [US1] Add `TestCommandErrorEnvelopeStdin` in `cmd/cmd_stdin_error_envelope_test.go` through all 12 actual commands listed in `specs/301-preserve-json-errors/research.md`; cover `filter: ` and generic malformed-key input × JSON/human × default/suppressed exit status, with valid caller prerequisites so the intended helper is reached; include a valid key and blank line before a bad key to verify processed zero-based indexing, exact guidance, no duplicate generic error after filter guidance, and no post-validation requests or continuation when exit status is zero.

### Implementation and validation

- [x] T006 [US1] Replace the search-validation direct exit in `cmd/delete_processinstance.go` with `handleCommandError(cmd, log, cfg.App.NoErrCodes, err)`, preserving the original validation position and error construction; make T004 pass.
- [x] T007 [US1] Add the actual `cmd *cobra.Command` parameter to `mergeAndValidateKeys` in `cmd/cmd_cli.go`, route both existing invalid-input expressions through `handleCommandError`, and atomically update callers in `cmd/get_incident.go`, `cmd/get_processinstance.go`, `cmd/expect_processinstance.go`, `cmd/update_processinstance.go`, `cmd/cancel_processinstance.go`, `cmd/delete_processinstance.go`, `cmd/resolve_processinstance.go`, `cmd/delete_processdefinition.go`, `cmd/resolve_incident.go`, `cmd/ops_analyse_slow_process_instances.go`, `cmd/ops_repair_incident.go`, and `cmd/ops_repair_processinstance.go`; retain return type, key ordering, wrappers, and every caller's existing `.Unique()` choice, and make T005 pass.
- [x] T008 [US1] Format touched Go files and run `go test ./cmd -run '^TestCommandErrorEnvelope(DeleteValidation|Stdin)' -count=1`; verify the tests execute and record the story result in `specs/301-preserve-json-errors/quickstart.md`.

**Checkpoint**: US1 is a usable validation-error increment without US2. Human reporting and exit suppression already have per-path coverage in this increment.

## Phase 4: User Story 2 — Structured Runtime Failures (Priority: P1)

**Goal**: Cluster retrieval and the additional audited full-contract execution paths preserve the shared error result.

**Independent Test**: Fail topology, version, license, process-definition retrieval/selector/search, XML-option validation, and embedded listing using local fixtures. Every corrected reachable branch gets JSON/human and default/suppressed exit checks with exact detail and no duplicate result.

### Tests and deterministic failure preparation

- [x] T009 [P] [US2] Add `TestCommandErrorEnvelopeCluster` in `cmd/get_cluster_error_envelope_test.go` using adjacent fixtures from `cmd/get_test.go`; cover topology/version/license HTTP 503 failures and representative malformed responses, separately captured streams, existing contextual messages, expected `failed` classifications and exit codes, and JSON/human × default/suppressed exits without adding backend requests.
- [x] T010 [P] [US2] Add `TestCommandErrorEnvelopeProcessDefinition` in `cmd/get_processdefinition_error_envelope_test.go` for by-key retrieval, selector lookup, paged-search failure, and XML option validation; cover JSON/human × default/suppressed exits for reachable combinations, `--xml --json` with and without a key, and corresponding human invalid-XML cases, preserving missing-key precedence and incompatible-option detail and proving no XML retrieval follows rejection.
- [x] T011 [P] [US2] Extract a small ordinary execution runner in `cmd/embed_list.go` accepting an explicit listing function, with production passing `embedded.List`; keep current direct error dispatch temporarily so failure tests can reproduce the defect, preserve bootstrap handling and success behavior, avoid global mutable injection, and verify existing `TestEmbedList` tests in `cmd/embed_test.go` pass.
- [x] T012 [US2] After T011, add `TestCommandErrorEnvelopeEmbedList` in `cmd/embed_list_error_envelope_test.go` invoking the same runner in scoped subprocesses with configured command context and injected listing error, empty list, and nonmatching-version files; cover JSON/human × default/suppressed exits, preserve `failed`/`local_precondition` for no matching files, assert single precise errors and no continuation, and retain real CLI success coverage in `cmd/embed_test.go` to prove production wiring.

### Implementation and validation

- [x] T013 [P] [US2] Route runtime failures through `handleCommandError` in `cmd/get_cluster_topology.go`, `cmd/get_cluster_version.go`, and `cmd/get_cluster_license.go`, preserving the existing wrapped error and no-error-code source in each handler; make T009 pass.
- [x] T014 [P] [US2] Route only XML-option validation, by-key retrieval, selector lookup, and paged-search failures through `handleCommandError` in `cmd/get_processdefinition.go`; preserve valid raw XML retrieval/write handlers and unreachable renderer-error branches, and make T010 pass without changing mode validation or success results.
- [x] T015 [P] [US2] Route the listing-error and no-matching-files branches of the runner in `cmd/embed_list.go` through `handleCommandError`, retaining the local-precondition wrapper, bootstrap path, renderer behavior, and version filtering; make T012 pass.
- [x] T016 [US2] Format touched Go files and run `go test ./cmd -run '^TestCommandErrorEnvelope(Cluster|ProcessDefinition|EmbedList)|TestGetCluster|TestGetProcessDefinition|TestEmbedList' -count=1`; record executed cases and results in `specs/301-preserve-json-errors/quickstart.md`.

**Checkpoint**: US2 can be validated independently of the stdin correction. The additional audited validation path belongs here under US2 acceptance scenario 3, avoiding an artificial fourth story.

## Phase 5: User Story 3 — Preserve Existing Workflows (Priority: P2)

**Goal**: Confirm that the correction preserves output precedence, non-full fallback, successful execution, human diagnostics, and exit policy.

**Independent Test**: Run the per-path matrices from US1/US2 plus mode-combination, fallback, and success regressions. Expected output/exit status must match the established behavior except for repaired JSON omissions.

### Compatibility tests

- [x] T017 [P] [US3] Add `TestCommandErrorEnvelopeModes` in `cmd/cmd_error_envelope_modes_test.go` for representative corrected validation and runtime failures with JSON+quiet, JSON+keys-only where supported, supported automation, keys-only alone, and quiet alone; preserve JSON precedence and explicit JSON in quiet mode, zero result bytes for non-JSON failures, existing stderr error visibility, exit suppression, and existing unsupported-automation rejection without bypassing its guard.
- [x] T018 [P] [US3] Add `TestCommandErrorEnvelopeFallback` in `cmd/cmd_error_envelope_fallback_test.go` using limited/unsupported command fixtures passed through the shared stdin helper plus representative real non-full config/embed commands; preserve ordinary error output and exit policy with JSON requested and `--no-err-codes` on/off, and verify an already-correct full-contract failure still emits exactly one envelope; do not invent a current non-full production caller.
- [x] T019 [P] [US3] Add or extend success regressions in `cmd/cmd_error_envelope_success_test.go` for valid flag/stdin key merging, existing caller deduplication, cluster JSON/human success, process-definition list/key/raw XML, and embedded list; reuse existing fixtures and assertions from `cmd/get_test.go`, `cmd/get_processdefinition_test.go`, and `cmd/embed_test.go`, extending only coverage gaps, and verify existing tenantContext attachment/omission without extra enrichment requests.

### Integration and validation

- [x] T020 [US3] Audit the final production diff against `cmd/command_contract.go`, `cmd/cmd_views_contract.go`, `c8volt/ferrors/errors.go`, and the exclusions in `specs/301-preserve-json-errors/research.md`; ensure no schema/class/exit-policy or capability-support changes, duplicate renderer invocation, bootstrap/parsing changes, raw-XML changes, or post-error execution, and record the audit result in `specs/301-preserve-json-errors/quickstart.md`.
- [x] T021 [US3] Run `go test ./cmd -run '^TestCommandErrorEnvelope|TestGetCluster|TestGetProcessDefinition|TestEmbedList|TestCommandCapability|TestOutputModes' -count=1` after US1/US2 integration; verify every corrected branch has human/JSON and exit-suppression evidence and record results in `specs/301-preserve-json-errors/quickstart.md`.

**Checkpoint**: All three stories meet their acceptance criteria. US3 adds compatibility evidence rather than a new error policy; fix any failures within the owning US1/US2 files before marking it complete.

## Phase 6: Polish and Cross-Cutting Validation

**Purpose**: Align user guidance and complete required verification.

- [x] T022 [P] Update `README.md` machine-contract guidance with the single command-execution error envelope, stderr human reporting, `--no-err-codes` retaining failure meaning, and the explicit bootstrap/parsing exclusion; do not promise broader command support.
- [x] T023 [P] Update relevant help descriptions/examples in `cmd/delete_processinstance.go`, `cmd/get_cluster_topology.go`, `cmd/get_cluster_version.go`, `cmd/get_cluster_license.go`, `cmd/get_processdefinition.go`, and `cmd/embed_list.go`, plus affected stdin caller help where clarification is necessary; preserve annotations, identifiers, flags, and aliases, and update corresponding help assertions in `cmd/get_test.go` and `cmd/embed_test.go` where affected.
- [x] T024 After T022/T023, format all touched Go files and regenerate CLI references with `make docs-content` from `Makefile`; inspect `docs/cli/` and the generated homepage for accuracy and unrelated churn, never hand-editing generated pages.
- [ ] T025 Follow `specs/301-preserve-json-errors/quickstart.md` to run the standalone-binary validation smoke scenarios with separately captured stdout/stderr and process statuses; record actual results and reconcile any stale guide commands or proposed test names with the completed implementation.
- [ ] T026 Run the final focused command checks from `specs/301-preserve-json-errors/quickstart.md`, then `make test` from `Makefile` (`go test ./... -race -count=1`) and `git diff --check`; record results or explicit environmental blockers in the quickstart, and mark completed tasks in `specs/301-preserve-json-errors/tasks.md` only when their evidence is satisfied.

## Dependencies and Execution Order

```text
T001 → T002 → T003
                 ├─ US1: T004/T005 → T006 → T007 → T008
                 ├─ US2: T009 → T013 ────────────────────┐
                 │       T010 → T014 ───────────────────┼→ T016
                 │       T011 → T012 → T015 ────────────┘
                 └─ US3 tests: T017/T018/T019 (after relevant corrected paths)
US1 + US2 + US3 tests → T020 → T021
T021 → T022/T023 → T024 → T025 → T026
```

- Setup and foundation block all stories. T003 support is established once and treated as read-only by concurrent story work.
- US1 and US2 do not depend on each other functionally. Each requires its tests before its own dispatch corrections. For a single implementer, follow task-number order.
- T006 and T007 are sequential because both touch `cmd/delete_processinstance.go`. T007 changes the helper and all callers as one compiling unit.
- T011 is a behavior-preserving testability step, not the embedded error correction; T012 must demonstrate the missing-envelope defect before T015 fixes it.
- US3 compatibility cases may be authored after foundation, but their completion gate T021 depends on US1 and US2. Do not duplicate the full per-path human/no-error-code matrices already owned by those stories.
- Validation tasks T008, T016, T020, T021, T025, and T026 share the quickstart evidence file; serialize those writes.
- `[P]` implementation tasks T013–T015 require their respective regression tests to exist and reproduce the defect first. Documentation tasks wait for final behavior.

## Parallel Examples by Story

### US1

After T003, T004 (`cmd/delete_processinstance_error_envelope_test.go`) and T005 (`cmd/cmd_stdin_error_envelope_test.go`) can be authored independently. Then perform the overlapping production edits T006 and T007 sequentially.

### US2

After T003, T009, T010, and T011 operate in separate files. After their prerequisite tests, T013 (cluster files), T014 (`cmd/get_processdefinition.go`), and T015 (`cmd/embed_list.go`) can proceed independently. T012 waits for T011.

### US3

Once the corrected paths and shared assertions are available, T017, T018, and T019 use separate compatibility test files. Integrate all three before T020/T021. Tests using package-global flags remain serial within a process even if file authoring is concurrent.

These examples describe safe implementation opportunities; task generation does not start additional agents or execution loops.

## Requirement Coverage

| Requirement | Tasks |
| --- | --- |
| FR-001, FR-002: corrected full-contract scope | T001, T004–T007, T009–T015 |
| FR-003, FR-005: single result and precise existing detail | T003–T005, T009–T012, T017–T020 |
| FR-004: invalid input versus existing runtime outcomes | T003–T005, T009–T015 |
| FR-006, FR-007: human stderr and exit policy | Per-path tests T004, T005, T009, T010, T012; T017, T018, T021 |
| FR-008, FR-009: success, modes, eligibility, schema | T003, T007, T014, T015, T017–T020 |
| FR-010, FR-011: command-level and full validation | T002–T005, T008–T012, T016–T021, T025, T026 |
| FR-012: user documentation | T022–T025 |
| SC-001–SC-005 | Per-path assertions, compatibility tests, and final validation T026 |

## Implementation Strategy

1. Complete setup and shared test support.
2. Deliver US1 as the MVP: corrected deletion and stdin validation, with full per-path stream/exit checks. Validate T008 before adding more behavior. This is an incremental checkpoint, not completion of issue #301.
3. Deliver US2 independently using local runtime fixtures and the narrow embedded runner seam.
4. Complete US3 compatibility verification and integrate both increments.
5. Update docs, regenerate references, run the smoke guide and full race suite. No commit or release is implied by this task list. Any implementation commit must satisfy the repository's relevant checks and full-test requirement.

No database, facade model, service mechanics, generated client changes, new error framework, or unrelated refactor is needed. Re-read `specs/ralph-implementation-rules.md` before any later Ralph-driven execution.
