# C89 Real-State Integration Gaps

This is a planning artifact for missing command capabilities and embedded BPMN assets that block deeper Camunda 8.9 real-state coverage. It is not generated by integration tests and must not be treated as runtime evidence.

## Current Gaps

| Topic | Gap Type | Required State Or Capability | Blocked Proof | Affected Commands | Runtime Behavior Until Closed | Affected Versions |
| --- | --- | --- | --- | --- | --- | --- |
| Job timeout mutation | command capability | Create or activate a job that Camunda accepts for confirmed timeout updates through c8volt setup | Confirmed timeout post-state proof after the mutation completes | `update job --timeout`, `get job` | Keep dry-run and supported submitted paths; skip confirmed timeout proof when no activated job prerequisite exists | 8.9 |
| BPMN error mutation | embedded BPMN asset | C89 process with a service task job and catchable BPMN error path for `ErrorTimerCode` | Confirmed BPMN error process-state proof after the mutation completes | `update job --throw-bpmn-error`, `get job`, `expect process-instance` | Keep dry-run proof and unchanged-job follow-up; skip confirmed mutation proof until fixture exists | 8.9 |
| BPMN error setup | command capability | c8volt setup/start path that deterministically produces a job eligible for the catchable BPMN error path | End-to-end BPMN error mutation from CLI-only setup | `deploy embedded`, `run process`, `update job --throw-bpmn-error` | Use existing deploy/run commands where possible; skip confirmed proof if no eligible job can be produced | 8.9 |
| Listener variants | embedded BPMN asset | Task-listener or richer listener fixture beyond execution-listener jobs from `C89_SimpleServiceTask.bpmn` | Broader listener coverage across listener kinds | `walk process-instance --with-listeners`, `get element --with-listeners`, `ops analyse slow-process-instances --with-listeners` | Keep current execution-listener proof; add skipped prerequisite evidence for unsupported listener variants | 8.9 |
| Process-definition and orphan purge candidates | command setup | Disposable process definitions and orphan-like process-instance dependency states with controlled dependents | Confirmed process-definition purge, orphan purge, and delete blocked/deleted reporting | `ops purge all-process-definitions`, `ops purge orphan-process-instances`, `delete process-definition` | Keep incident-selected purge and explicit process-instance cancel/delete live-covered; skip unavailable process-definition/orphan branches | 8.9 |
| Durable standalone resolve candidate | embedded BPMN asset or command setup | Incident whose root cause can be cleared before `resolve pi` or that does not immediately recreate after incident resolution | Confirmed resolve post-state with no active incident after resolution-only command flow | `resolve incident`, `resolve process-instance`, `get incident` | Current destructive slice proves resolve dry-run and confirmed submission on a real self-recreating incident; durable clearing is covered through `ops repair` | 8.9 |
| Repair partial failures | embedded BPMN asset or command setup | Mixed repair targets with valid, stale, missing, malformed, and already-mutated states | `ops repair` fail-fast and partial accounting on real resources and report rows | `ops repair incident`, `ops repair process-instance` | Keep dry-run/report parity checks; skip unavailable mixed-state branches | 8.9 |
| Expect state-only identity | command behavior | State-only `expect process-instance` JSON rows that expose stable key and ok fields like incident expectations | Machine-output identity correlation for state-only expect rows | `expect process-instance` | Keep existing semantic assertions; track as product behavior gap if command output lacks identity fields | 8.9 |
| Structured variable payload setup | embedded BPMN asset or command setup | Process instance carrying nested object, array, boolean, numeric, string, and null variables through c8volt setup | Volume and real-state variable rendering, filtering, and mutation proof over representative payload shapes | `run process`, `update process-instance`, `get process-instance` | Keep existing variable-shape assertions where seeded data exists; treat richer shapes as skipped-prerequisite until setup is available | 8.9 |
| Slow-duration analysis candidate | embedded BPMN asset or command setup | Process instance and element timeline that reliably exceeds duration thresholds without timing flakiness | Non-empty `ops analyse slow-process-instances` duration filter proof over deterministic slow candidates | `ops analyse slow-process-instances`, `walk process-instance` | Keep current explicit/search/timeline coverage; skip truly slow-duration proof until deterministic setup exists | 8.9 |
| Smoke-test incident and job-state branches | embedded BPMN asset or command setup | Process instances with active incidents and controllable job states suitable for smoke-test branch validation | `ops execute smoke-test` incident and job-state report branches on non-empty real candidates | `ops execute smoke-test`, `get incident`, `get job`, `ops repair incident` | Keep current smoke-test dry-run/confirmed no-wait coverage; skip incident/job-state branches until setup exists | 8.9 |
| Aged retention and broad retention filters | command setup | Completed process instances with controllable age, process-definition version, and version tag metadata | Retention deletion and no-wait proof for aged candidates plus `--pd-version` and `--pd-version-tag` filters | `ops execute retention-policy`, `delete process-instance`, `get process-instance` | Keep `--retention-days 0` completed-candidate proof; skip aged and metadata-filter proof until setup exists | 8.9 |
| Cleanup-failed and report edge candidates | command setup | Disposable resources that produce retained, cleanup-failed, overwrite-conflict, and preserve-report branches deterministically | Ops report rows for cleanup failures, preserved resources, overwrite protection, notices, and errors | `ops execute retention-policy`, `ops purge all-process-definitions`, `ops purge orphan-process-instances`, `ops purge process-instances-with-incidents` | Keep current stdout/report parity for live-covered branches; skip deterministic cleanup-failed branches until setup exists | 8.9 |
| Stdin pipeline mixed-input targets | command behavior and setup | Keys-only producers and stdin consumers with empty, duplicate, whitespace-padded, malformed, missing, valid, dry-run, and confirmed mutation inputs | Pipeline proof that clean producer output safely feeds destructive stdin consumers without hangs or stdout contamination | `get * --keys-only`, `cancel process-instance`, `delete process-instance`, `resolve incident`, `resolve process-instance` | Keep existing family and volume pipeline coverage; close remaining stdin combinations through 257-owned follow-up issues | 8.9 |
| Auxiliary resource and tenant targets | command setup | Suite-owned numeric resource identifiers, user-task or element-instance targets, and tenant-specific disposable data where supported | Positive and negative coverage for resource, user-task, element, and tenant-sensitive command paths without relying on dirty global data | `get resource`, `get task`, `get element`, `get tenant`, `run process` | Keep current read/empty behavior; add setup only where Camunda 8.9 and c8volt expose stable target creation | 8.9 |

## Maintenance Rules

- Add a row when a real-state scenario cannot prove a required behavior because a setup command or embedded BPMN asset is missing.
- Remove or update a row only after the corresponding runtime test proves the behavior against real Camunda state.
- Keep the runtime-test behavior column honest: skipped, dry-run-covered, partially live-covered, or live-covered.
- Keep future Camunda minor releases as additional rows or explicit scope updates rather than changing the 8.9 foundation silently.
- Use the `Affected Versions` column for the current 8.9 foundation and add explicit new rows or comma-separated minors when later Camunda releases need different setup or assertions.
