# C89 Real-State Integration Gaps

This is a planning artifact for missing command capabilities and embedded BPMN assets that block deeper Camunda 8.9 real-state coverage. It is not generated by integration tests and must not be treated as runtime evidence.

## Current Gaps

| Topic | Gap Type | Required State Or Capability | Blocks | Runtime Behavior Until Closed | Camunda Scope |
| --- | --- | --- | --- | --- | --- |
| Job timeout mutation | command capability | Create or activate a job that Camunda accepts for confirmed timeout updates through c8volt setup | Confirmed `update job --timeout` post-state proof | Keep dry-run and supported submitted paths; skip confirmed timeout proof when no activated job prerequisite exists | 8.9 |
| BPMN error mutation | embedded BPMN asset | C89 process with a service task job and catchable BPMN error path for `ErrorTimerCode` | Confirmed `update job --throw-bpmn-error` process-state proof | Keep dry-run proof and unchanged-job follow-up; skip confirmed mutation proof until fixture exists | 8.9 |
| BPMN error setup | command capability | c8volt setup/start path that deterministically produces a job eligible for the catchable BPMN error path | End-to-end BPMN error mutation from CLI-only setup | Use existing deploy/run commands where possible; skip confirmed proof if no eligible job can be produced | 8.9 |
| Listener variants | embedded BPMN asset | Task-listener or richer listener fixture beyond execution-listener jobs from `C89_SimpleServiceTask.bpmn` | Broader `--with-listeners` coverage across listener kinds | Keep current execution-listener proof; add skipped prerequisite evidence for unsupported listener variants | 8.9 |
| Purge and delete candidates | command setup | Disposable deployments, process definitions, incidents, and process instances with controlled dependencies | Confirmed purge/delete post-state and blocked-with-dependent reporting | Build candidates through c8volt first; skip candidate-specific proof when setup cannot be created safely | 8.9 |
| Repair partial failures | embedded BPMN asset or command setup | Mixed repair targets with valid, stale, missing, malformed, and already-mutated states | `ops repair` fail-fast and partial accounting on real resources | Keep dry-run/report parity checks; skip unavailable mixed-state branches | 8.9 |
| Expect state-only identity | command behavior | State-only `expect process-instance` JSON rows that expose stable key and ok fields like incident expectations | Machine-output identity correlation for state-only expect rows | Keep existing semantic assertions; track as product behavior gap if command output lacks identity fields | 8.9 |

## Maintenance Rules

- Add a row when a real-state scenario cannot prove a required behavior because a setup command or embedded BPMN asset is missing.
- Remove or update a row only after the corresponding runtime test proves the behavior against real Camunda state.
- Keep the runtime-test behavior column honest: skipped, dry-run-covered, partially live-covered, or live-covered.
- Keep future Camunda minor releases as additional rows or explicit scope updates rather than changing the 8.9 foundation silently.
