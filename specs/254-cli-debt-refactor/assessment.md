# CLI Debt Refactor Assessment

Feature: 254-cli-debt-refactor
Created: 2026-07-24

## Phase 1 Setup Findings

### Context And Rules Review

- Reviewed `specs/ralph-implementation-rules.md` against `specs/254-cli-debt-refactor/spec.md`; no conflict found.
- Binding ownership boundary for this feature: `cmd` owns flags, validation, prompts, render-mode selection, stdout/stderr rendering, command metadata, and help; facades map public inputs and errors; internal services own backend paging, traversal, frozen discovery, mutation planning, polling, retries, and worker execution.
- The feature's first deliverable is a checked-in assessment. Refactor tasks must not start until the full assessment structure and all 55 command-node classifications are complete.

### Basic Paging Implementations

Reviewed files: `cmd/get_job_search.go`, `cmd/get_element_search.go`, `cmd/get_incident_search.go`, and `cmd/get_processinstance_search.go`.

- `get job` and `get element` both implement command-owned offset page loops with the same mechanics: compute initial page request, call `Search*Page`, trim items to `--limit`, optionally stream one-line or keys-only output, collect JSON results, prompt when more data remains, and compute total fallback by walking pages.
- `get incident` follows the same command-owned pattern with incident-specific concerns: cursor fallback via `EndCursor`, `--process-instance-keys-only`, incident detail row rendering, and a shared continuation-state vocabulary borrowed from process-instance paging.
- `get process-instance` is the highest-risk basic read path. It owns backend page traversal, local filters, direct incident-index search strategy, enrichment invocation boundaries, incremental rendering, warning-stop handling, and verbose progress.
- Candidate mechanics for later ownership reduction: page walking, page-size normalization, total fallback, limit trimming, cursor/offset advancement, direct incident-index candidate lookup, and process-instance local compatibility filtering.
- CLI-owned behavior to preserve during later refactors: interactive prompts, one-line and keys-only streaming, JSON collection into one document, found summaries, warning text, and render-mode decisions.

### Process-Instance Mutation Paging

Reviewed files: `cmd/get_processinstance_paging.go`, `cmd/cancel_processinstance.go`, and `cmd/delete_processinstance.go`.

- Original baseline: `processPISearchPagesWithAction` centralized command-level cancel/delete paging mechanics: per-page search, local `--limit` trimming, continuation state, first-page confirmation, dry-run aggregation, partial-stop progress, and page-by-page action invocation.
- Original baseline: `cancel process-instance` mapped search pages directly to keys and submitted page actions through command-owned planning/render functions. Confirmation safety depended on first-page impact counts and had to be preserved.
- Original baseline: `delete process-instance` had an extra planning path for non-dry-run search mode: it walked all selected pages, built delete previews without mutating, aggregated the frozen delete plan, checked non-final scope, prompted once, then deleted the frozen roots.
- US2 result: search-selected cancel/delete discovery traversal and page-level dry-run dependency planning now run below command ownership through `c8volt/process.PlanProcessInstanceMutationPages` and `internal/services/processinstance.PlanProcessInstanceMutationPages`. `cmd` retains confirmation prompts, progress rendering, dry-run presentation, and final mutation/result handling. Delete's frozen-scope safety remains intentionally different from cancel.

### Ops Workflow Ownership

Reviewed files under `internal/services/ops/` plus command wiring and renderers under `cmd/ops_*.go` and `cmd/cmd_views_ops_*.go`.

- Ops services already own most workflow mechanics: incident purge freezes incident-derived process-instance candidates; all-process-definition purge freezes process-definition candidates; retention policy delegates retention discovery to `internal/services/processinstance`; repair workflows freeze either incident or process-instance targets before mutation; slow-process analysis performs read-only discovery and enrichment below `cmd`.
- Several ops paths already use bounded worker helpers for independent work: repair execution uses `toolx/pool.ExecuteSlice`, retention dependency expansion uses `pool.ExecuteSlice`, and process-definition purge delegates worker-aware planning to process-definition services.
- Similar discovery loops are not automatically equivalent. Incident purge tracks skipped incidents and duplicate process-instance candidates; all-process-definition purge tracks latest/all-version process-definition semantics and active-instance impact; retention policy tracks retention boundaries and non-final root skips; slow-process analysis tracks read-only duration/enrichment filters.
- Later refactor work should extract only mechanics with matching semantics. The safest near-term ops targets are progress-summary rendering consistency and targeted high-volume characterization, not a generic ops discovery abstraction.

### Output, Activity, And Capability Helpers

Reviewed files: `cmd/root.go`, `cmd/command_contract.go`, `toolx/logging/activity.go`, `cmd/cmd_views_ops_repair.go`, `cmd/cmd_views_ops_purge_processinstances_with_incidents.go`, and `cmd/cmd_views_ops_purge_all_processdefinitions.go`.

- Root command creates the shared activity writer with `indicatorEnabled(cmd, cfg)` and installs it in context for HTTP and command activity sinks. Activity output is routed through stderr and gated by terminal interactivity.
- `toolx/logging.ActivityWriter` clears transient spinner output before normal writes, tracks nested activity references, supports message updates, and is disabled when no interactive terminal is present.
- Capability metadata is annotation-driven in `cmd/command_contract.go`; command support defaults are inferred from command tree shape unless commands set mutation, contract support, automation support, output modes, or required-flag metadata explicitly.
- Ops renderers already distinguish user-limited discovery in compact human output and hide complete discovery page detail unless `--verbose` is enabled. JSON/report paths retain complete discovery state through shared result rendering.
- Later policy work should align basic search progress with the ops pattern: default compact output, verbose durable page details away from stdout, and machine-output silence for JSON, keys-only, quiet, automation, and no-indicator scenarios.

## Command Node Assessment

Assessment source: live `capabilities --json` command tree on branch `254-cli-debt-refactor`.

Required columns: path, aliases, family, mutation, contract support, automation support, output modes, paging behavior, mutation behavior, activity indicator behavior, durable progress behavior, JSON and keys-only constraints, behavior ownership, execution style, and high-volume performance risk.

| Path | Aliases | Family | Mutation | Contract | Automation | Output Modes | Paging | Mutates | Activity | Durable Progress | Machine Constraints | Ownership | Execution Style | Risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `cancel` | c, cn, stop, abort | grouping mutation | state_changing | limited | unsupported | one-line | none | routes state changes | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `cancel process-instance` | pi | basic mutation | state_changing | full | full | one-line | service-owned process-instance search paging and page-level dry-run planning for selector mode | cancels process instances | shared activity through facade and HTTP context | page progress, prompt, dry-run and result summaries | no JSON or keys-only contract currently advertised; automation must avoid unexpected prompt | cmd owns flags, confirmation, prompts, rendering, and final result handling; facade/service own search traversal, limit trimming, dependency planning, and keyed cancel execution | mixed service planning visitor plus bounded worker mutation | high |
| `capabilities` | - | discovery | read_only | limited | full | json, one-line | command tree traversal only | no | no | none | JSON is the preferred machine document; one-line is summary only | cmd owns capability document generation from Cobra annotations | serial local traversal | low |
| `config` | cfg | config grouping | read_only | unsupported | unsupported | one-line | none | no | no | none | unsupported machine contract | cmd owns grouping and routing | serial dispatch | low |
| `config show` | - | config | read_only | unsupported | unsupported | one-line | none | no | no | none | unsupported machine contract | cmd owns config rendering and compatibility shortcuts | serial local read | low |
| `config template` | - | config | read_only | unsupported | unsupported | one-line | none | no | no | none | unsupported machine contract | cmd owns static template rendering | serial local render | low |
| `config test-connection` | - | config | read_only | unsupported | unsupported | json | none | no | shared HTTP activity possible | none | JSON is command-specific, not shared contract | cmd owns bootstrap and result rendering; service owns HTTP probe | serial remote probe | medium |
| `config validate` | - | config | read_only | unsupported | unsupported | one-line | none | no | no | none | unsupported machine contract | cmd owns config validation output | serial local validation | low |
| `delete` | d, del, remove, rm | grouping mutation | state_changing | limited | unsupported | one-line | none | routes state changes | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `delete process-definition` | pd | basic mutation | state_changing | full | full | one-line | service-owned process-definition and process-instance impact discovery | deletes definitions and may cancel instances | shared activity through facade and HTTP context | dry-run, confirmation, impact, and result summaries | no JSON or keys-only contract currently advertised; automation requires explicit confirmation behavior | cmd owns flags, confirmation, rendering; services own impact planning and delete execution | mixed bounded worker planning and mutation | high |
| `delete process-instance` | pi | basic mutation | state_changing | full | full | one-line | service-owned search paging and page-level dry-run planning with command-owned frozen aggregate confirmation in selector mode | deletes process instances and may cancel first | shared activity through facade and HTTP context | page progress, frozen plan, prompt, dry-run, and result summaries | no JSON or keys-only contract currently advertised; automation must not prompt unexpectedly | cmd owns flags, frozen plan confirmation, force rejection, rendering, and final result handling; facade/service own search traversal, limit trimming, dependency planning, and keyed delete/cancel execution | mixed service planning visitor plus bounded worker mutation | high |
| `deploy` | dep | grouping mutation | state_changing | limited | unsupported | one-line | none | routes deployments | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `deploy process-definition` | pd | basic mutation | state_changing | full | full | one-line | none | deploys process definitions and may run instance | shared activity through facade and HTTP context | accepted-result summary | no JSON or keys-only contract currently advertised | cmd owns flags and rendering; facade/service own deploy and optional run mechanics | serial remote submit with optional follow-up | medium |
| `embed` | em, emb | embed grouping | state_changing | limited | unsupported | one-line | local embedded asset traversal only | routes embed workflows | no remote activity | none | child commands vary and legacy commands are not full contract | cmd owns grouping and routing | serial dispatch | low |
| `embed deploy` | dep | embed compatibility | state_changing | unsupported | unsupported | one-line | embedded asset traversal when all assets requested | deploys embedded definitions | shared activity through deploy facade possible | accepted-result summary | unsupported machine contract | cmd owns embed selection and rendering; deploy facade owns remote submit | serial or small local loop | medium |
| `embed export` | exp, extract | embed compatibility | state_changing | unsupported | unsupported | one-line | embedded asset traversal when all assets requested | writes local files | no remote activity | export summary | unsupported machine contract | cmd owns embed selection, file overwrite checks, and rendering | serial local file writes | low |
| `embed list` | ls | embed read | read_only | full | unsupported | json | local embedded asset traversal | no | no | optional detail output | JSON output must remain one valid document | cmd owns embedded inventory rendering | serial local traversal | low |
| `expect` | e, exp | grouping read | read_only | limited | unsupported | one-line | none | no | no direct remote work | none | child command owns contract | cmd owns grouping and routing | serial dispatch | low |
| `expect process-instance` | pi | read wait workflow | read_only | full | unsupported | one-line | keyed polling only | no | shared activity and polling output possible | wait progress and final state summary | automation unsupported for wait commands | cmd owns wait flags and rendering; facade/service own polling and state checks | serial polling with bounded key handling | medium |
| `get` | g, read | grouping read | read_only | limited | unsupported | one-line | none | no | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `get cluster` | - | grouping read | read_only | limited | unsupported | one-line | none | no | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `get cluster license` | licence | basic read | read_only | full | unsupported | json | none | no | shared HTTP activity possible | none | JSON output must remain one valid document | cmd owns rendering; service owns remote license lookup | serial remote lookup | low |
| `get cluster topology` | - | basic read | read_only | full | unsupported | json | none | no | shared HTTP activity possible | none | JSON output must remain one valid document | cmd owns rendering; service owns remote topology lookup | serial remote lookup | low |
| `get cluster version` | - | basic read | read_only | full | unsupported | one-line, json | optional broker list in one response | no | shared HTTP activity possible | none | JSON output must remain one valid document | cmd owns rendering; service owns version lookup | serial remote lookup | low |
| `get element` | ei | basic read | read_only | full | full | one-line | service-owned offset paging, total fallback, and limit trimming in search mode | no | shared HTTP activity possible | verbose page progress, prompts, found summary | inherited JSON and keys-only flags must stay clean even though output modes report one-line | cmd owns flags, prompts, and rendering; facade/service own page traversal, size capping, limit trimming, and totals | serial service page visitor with optional listener enrichment | high |
| `get incident` | incidents, inc | basic read | read_only | full | full | one-line | service-owned cursor or offset paging, cursor fallback, local filtering, and process-instance-key projection | no | shared HTTP activity possible | verbose page progress, prompts, process-instance-key summary | inherited JSON, keys-only, and pi-keys-only output must stay clean | cmd owns flags, prompts, and rendering; facade/service own traversal, cursor handling, filtering, projection, and totals | serial service page visitor with bounded keyed lookup paths | high |
| `get job` | - | basic read | read_only | full | full | one-line | service-owned offset paging, total fallback, and limit trimming in search mode | no | shared HTTP activity possible | verbose page progress, prompts, found summary | inherited JSON and keys-only flags must stay clean even though output modes report one-line | cmd owns flags, prompts, and rendering; facade/service own page traversal, size capping, limit trimming, and totals | serial service page visitor | high |
| `get process-definition` | pd, pds | basic read | read_only | full | unsupported | json | service-owned list or latest lookup | no | shared HTTP activity possible | none | JSON output must remain one valid document | cmd owns flags and rendering; service owns lookup/search | serial remote lookup | medium |
| `get process-instance` | process-instances, pi, pis | basic read | read_only | full | full | one-line | service-owned search paging, local compatibility filtering, direct incident-index strategy, limit trimming, and total fallback | no | shared HTTP activity possible | verbose page progress, prompts, warning-stop and found summaries | inherited JSON and keys-only flags must stay clean; automation must not prompt | cmd owns flags, validation, mode selection, prompts, rendering, and enrichment display boundaries; facade/service own traversal, query strategy, compatibility filtering, and totals | mixed service page visitor plus bounded enrichment/lookup paths | high |
| `get resource` | r | basic read | read_only | full | unsupported | one-line | none | no | shared HTTP activity possible | none | one-line contract only | cmd owns flags and rendering; service owns resource lookup | serial remote lookup | low |
| `get tenant` | tenants | basic read | read_only | full | full | one-line | service-owned tenant listing/filter | no | shared HTTP activity possible | none | inherited JSON output must remain one valid document where used | cmd owns flags and rendering; service owns tenant lookup/list | serial remote lookup | low |
| `ops` | operations | high-level ops grouping | state_changing | limited | unsupported | one-line | none | routes ops workflows | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `ops analyse` | analyze | ops grouping | read_only | limited | unsupported | one-line | none | no | no direct remote work | none | child command owns machine contract | cmd owns grouping and alias routing | serial dispatch | low |
| `ops analyse slow-process-instances` | slow-pi, spi | high-level ops read | read_only | full | full | one-line, json, keys-only | service-owned process-instance discovery and detail enrichment | no | shared activity through facade and HTTP context | discovery summary and optional detail progress | JSON and keys-only must stay clean | cmd owns flags and rendering; ops/process services own discovery, filtering, enrichment | mixed service discovery with bounded explicit-key lookup and enrichment paths | high |
| `ops execute` | - | ops grouping | state_changing | limited | unsupported | one-line | none | routes execute workflows | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `ops execute retention-policy` | ret-pol, rp | high-level ops mutation | state_changing | full | full | one-line | service-owned retention discovery over process-instance pages | deletes or cancels eligible instances | shared activity through facade and HTTP context | dry-run, discovery, limit, report, and mutation summaries | no JSON or keys-only contract currently advertised; report files hold structured detail | cmd owns flags, confirmation/force semantics, report rendering; ops/process services own retention discovery and execution | mixed service discovery with bounded worker planning and mutation | high |
| `ops execute smoke-test` | st | high-level ops mutation | state_changing | full | full | one-line | count-driven local workflow rather than backend paging | deploys/runs/cancels/deletes smoke resources | shared activity through facade and HTTP context | dry-run, cleanup, and report summaries | no JSON or keys-only contract currently advertised; report files hold structured detail | cmd owns flags and rendering; ops service owns smoke workflow and cleanup | mixed bounded worker cleanup | medium |
| `ops purge` | - | ops grouping | state_changing | limited | unsupported | one-line | none | routes purge workflows | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `ops purge all-process-definitions` | all-pds, apd | high-level ops mutation | state_changing | full | full | one-line, json | service-owned process-definition discovery and impact planning | deletes definitions and related instances | shared activity through facade and HTTP context | discovery, dry-run, report, and mutation summaries | JSON/report output must stay clean and distinguish user-limited discovery | cmd owns flags, confirmation/force, rendering; ops/process-definition services own discovery and execution | mixed bounded planning and mutation | high |
| `ops purge orphan-process-instances` | orphan-pi, opi | high-level ops mutation | state_changing | full | full | one-line | service-owned orphan candidate discovery | deletes or cancels orphan instances | shared activity through facade and HTTP context | discovery, dry-run, report, and mutation summaries | no JSON or keys-only contract currently advertised; report files hold structured detail | cmd owns flags, confirmation/force, rendering; ops/process services own discovery and execution | mixed service discovery and bounded worker mutation | high |
| `ops purge process-instances-with-incidents` | pi-with-incidents, piwi | high-level ops mutation | state_changing | full | full | one-line, json | service-owned incident discovery and process-instance candidate freezing | deletes or cancels incident-linked instances | shared activity through facade and HTTP context | discovery, duplicate/skipped candidate, report, and mutation summaries | JSON/report output must stay clean and distinguish user-limited discovery | cmd owns flags, confirmation/force, rendering; ops/incident/process services own discovery and execution | mixed bounded discovery and mutation | high |
| `ops repair` | - | ops grouping | state_changing | limited | unsupported | one-line | none | routes repair workflows | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `ops repair incident` | inc | high-level ops mutation | state_changing | full | full | one-line, json | service-owned incident discovery and repair planning | resolves incidents and may update jobs/variables | shared activity through facade and HTTP context | discovery, dry-run, report, and mutation summaries | JSON/report output must stay clean | cmd owns flags and rendering; ops service owns discovery, planning, and execution | mixed bounded worker repair | high |
| `ops repair process-instance` | pi, pis, process-instances | high-level ops mutation | state_changing | full | full | one-line, json | service-owned process-instance discovery and incident repair planning | repairs incidents for selected instances | shared activity through facade and HTTP context | discovery, dry-run, report, and mutation summaries | JSON/report output must stay clean | cmd owns flags and rendering; ops/process services own discovery, planning, and execution | mixed bounded worker repair | high |
| `resolve` | res | grouping mutation | state_changing | limited | unsupported | one-line | none | routes resolution workflows | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `resolve incident` | inc | basic mutation | state_changing | full | full | one-line | keyed batch only | resolves incidents | shared activity through facade and HTTP context | dry-run and result summaries | no JSON or keys-only contract currently advertised | cmd owns flags and rendering; facade/service own per-key mutation | bounded worker mutation | medium |
| `resolve process-instance` | pi | basic mutation | state_changing | full | full | one-line | keyed batch only | resolves related incidents | shared activity through facade and HTTP context | dry-run and result summaries | no JSON or keys-only contract currently advertised | cmd owns flags and rendering; facade/service own per-key incident resolution | bounded worker mutation | medium |
| `run` | r | grouping mutation | state_changing | limited | unsupported | one-line | none | routes start workflows | no direct remote work | none | child command owns machine contract | cmd owns grouping and routing | serial dispatch | low |
| `run process-instance` | pi | basic mutation | state_changing | full | full | one-line | process-definition selector expansion only | starts process instances | shared activity through facade and HTTP context | accepted-result summary | no JSON or keys-only contract currently advertised | cmd owns flags, selector validation, rendering; facade/service own start and confirmation behavior | mixed bounded worker creation for counts | medium |
| `update` | u | grouping mutation | state_changing | limited | unsupported | one-line | none | routes update workflows | no direct remote work | none | child commands own machine contracts | cmd owns grouping and routing | serial dispatch | low |
| `update job` | - | basic mutation | state_changing | full | full | one-line, json | none | updates job retries, timeout, or worker outcome | shared activity through facade and HTTP context | dry-run and accepted-result summaries | JSON requires dry-run, auto-confirm, or automation and must stay one document | cmd owns flags, confirmation gates, rendering; facade/service own mutation execution | serial remote submit | medium |
| `update process-instance` | pi | basic mutation | state_changing | full | full | one-line | keyed batch only | updates variables | shared activity through facade and HTTP context | dry-run and result summaries | no JSON or keys-only contract currently advertised | cmd owns flags and rendering; facade/service own variable lookup and mutation | bounded worker mutation | medium |
| `version` | - | compatibility read | read_only | full | unsupported | one-line | none | no | no | none | one-line contract only | cmd owns local build info rendering | serial local render | low |
| `walk` | w, traverse | grouping read | read_only | limited | unsupported | one-line | none | no | no direct remote work | none | child command owns machine contract | cmd owns grouping and routing | serial dispatch | low |
| `walk process-instance` | pi, pis | read traversal workflow | read_only | full | unsupported | one-line | keyed ancestry/descendant traversal and optional enrichment | no | shared activity through facade and HTTP context | traversal and enrichment summaries | automation unsupported; output must remain stable one-line/tree style | cmd owns flags and rendering; facade/service own traversal and enrichment | mixed traversal plus optional detail lookups | high |

## High-Risk Workflows And Duplicated Mechanics

- Highest-risk paths for high-volume behavior are `get process-instance`, `cancel process-instance`, `delete process-instance`, `ops execute retention-policy`, `ops purge all-process-definitions`, `ops purge orphan-process-instances`, `ops purge process-instances-with-incidents`, `ops repair incident`, `ops repair process-instance`, `ops analyse slow-process-instances`, and `walk process-instance`.
- Duplicated basic read mechanics are concentrated in `get job`, `get element`, `get incident`, and `get process-instance`: page-size normalization, page traversal, user-limit trimming, continuation decisions, total fallback, incremental rendering boundaries, found summaries, and prompt decisions.
- `get incident` adds cursor fallback and process-instance-key projection to the shared pattern; `get process-instance` adds local compatibility filtering, direct incident-index query strategy, enrichment boundaries, and warning-stop progress.
- Process-instance mutation debt was concentrated in selector mode for `cancel process-instance` and `delete process-instance`, where command code owned search-selected discovery traversal. US2 moved that traversal and page-level mutation planning into the process facade/service while retaining command-owned prompts, rendering, force checks, and final mutation decisions.
- Ops paths already keep most workflow mechanics below `cmd`; later work should focus on progress-summary consistency, performance characterization, and only extracting mechanics that match exact safety and reporting semantics.

## US2 Ownership Before/After

- Before US2: `get job`, `get element`, `get incident`, and `get process-instance` each reimplemented page traversal, limit trimming, total fallback, and continuation mechanics in command files.
- After US2: job, element, incident, and process-instance search traversal uses facade/service page visitor contracts; command files retain CLI validation, rendering, prompts, progress, and output-mode decisions.
- Before US2: search-selected `cancel process-instance` and `delete process-instance` owned page fetches, offset advancement, local limit trimming, and page-level dry-run dependency planning in command helpers.
- After US2: search-selected cancel/delete planning uses `PlanProcessInstanceMutationPages` in the process facade and `internal/services/processinstance`, which owns search traversal, limit trimming, and dependency expansion. `cancel` still mutates page by page after the preserved first confirmation, while `delete` still freezes all selected page plans before one aggregate confirmation and delete call.

## Intentional Differences And Non-Goals

- Do not flatten `delete process-instance` into page-by-page mutation; US2 preserved its non-dry-run frozen plan and single confirmation for the selected delete scope.
- Do not replace ops purge, repair, retention, smoke-test, and slow-analysis flows with one generic ops discovery abstraction; their candidate accounting, report fields, duplicate/skipped handling, dry-run semantics, and confirmation safety differ.
- Do not move CLI render-mode selection, prompts, confirmations, help text, or command capability metadata below `cmd`.
- Do not introduce a static audit framework for this feature. The assessment is a checked-in baseline, and validation tests only guard its completeness against the live command tree.
- Do not hand-edit generated Camunda clients as part of this refactor baseline.

## Performance Characterization Plan

- Characterize basic read paging with fake multi-page command tests for `get job`, `get element`, `get incident`, and `get process-instance`, including limits smaller than page size and final partial pages.
- Characterize destructive planning with cancel/delete selector tests that cover dry-run, auto-confirm, automation, partial failure, and worker controls.
- Characterize ops workflows with fake-latency or high-volume tests around incident repair, process-instance repair, process-instance-with-incidents purge, all-process-definition purge, orphan purge, retention policy, smoke-test cleanup, and slow-process analysis.
- Reuse `toolx/pool` for independent work only where result order, fail-fast behavior, configured worker limits, and no-worker-limit semantics remain explicit.

## US3 Performance Characterization Results

| Workflow | Evidence | Result |
| --- | --- | --- |
| Process-instance search and incident enrichment | `c8volt/process` fake-latency tests and `internal/services/processinstance/enrichment_test.go`; checkpoint command `go test ./cmd ./internal/services/ops ./c8volt/process -run 'Test.*(Latency|Concurrent|Performance|HighVolume|Workers|Cancel|Delete|Repair|Purge|Retention|Slow)' -count=1` | Direct incident enrichment and traversal incident-detail lookup use ordered bounded workers; result ordering remains deterministic while lookup call order is intentionally nondeterministic. |
| Cancel/delete dependency planning | `internal/services/processinstance/dryrun_test.go` and command worker-control tests in `cmd/cancel_test.go` / `cmd/delete_test.go` | `DryRunCancelOrDeletePlan` and search-selected page planning use `toolx/pool.ExecuteSlice` for ancestry/descendant traversal, preserve ordered plan output, and pass `--workers`, `--fail-fast`, and `--no-worker-limit` into planning and mutation. |
| Ops repair | `internal/services/ops/repair_test.go` and `cmd/ops_repair_incident_test.go` | Process-instance repair incident discovery and incident repair execution use bounded workers; CLI worker controls survive request/report construction. |
| Ops purge | `internal/services/ops/incident_purge_test.go` plus checkpoint validation | Incident purge freezes incident-derived candidates before planning; dependency expansion delegates to the shared bounded process-instance plan path and preserves deterministic root/affected ordering. |
| Retention policy | `internal/services/ops/retention_policy_test.go` | Retention discovery remains sequential by page, while independent ancestry and descendant planning use requested bounded workers and preserve deterministic seed/root output. |
| Slow-process analysis | `internal/services/ops/slow_process_analysis_test.go` | Explicit-key lookup now uses the default bounded worker policy. Process-definition discovery remains sequential by page because cursor/offset traversal is not independent work. |
| Job, element, and incident search | US2 facade/service page-collection tests and US3 checkpoint validation | Basic search traversal remains service-owned, limit-capped, and order-preserving. No extra concurrency was added because page advancement is sequential by offset/cursor and command output streams pages in order. |

Accepted retained serial paths:

- Cursor and offset page traversal remains serial for process-instance, incident, job, element, retention discovery, and slow-process process-definition discovery to avoid skipped/duplicated pages and uncontrolled request fan-out.
- Slow-process timeline calculation, detail filtering, comparison calculation, and final sorting remain local serial transforms after remote enrichment; no fake-latency test showed independent remote calls there beyond explicit-key lookup and existing enrichment paths.

## Success Criteria Evidence

| Criterion | Evidence |
| --- | --- |
| SC-001 | The command node assessment table contains all 55 command nodes and is guarded by `cmd/command_contract_test.go` plus docsgen assessment artifact validation. |
| SC-002 | US1 and US4 command tests cover clean JSON, keys-only, quiet, automation, prompt, and no-indicator behavior for changed command families; validation commands are recorded in `quickstart.md`. |
| SC-003 | The US3 performance characterization results table covers process-instance search/enrichment, cancel/delete planning, ops repair, ops purge, retention policy, slow-process analysis, and job/element/incident search. |
| SC-004 | US3 fake-latency and worker-control validation showed bounded-worker improvements or retained serial page traversal with documented safety reasons; no changed high-volume workflow has an undocumented slowdown. |
| SC-005 | Cancel/delete and ops destructive workflow tests cover confirmation, auto-confirm or automation controls, force behavior, partial/error handling, worker controls, and deterministic ordered reports where those workflows changed. |
| SC-006 | US4 updated command help, generated CLI docs, README/operator docs, and capability metadata for `--batch-size`, `--limit`, progress, clean machine output, automation support, and destructive worker controls. |
| SC-007 | The command node table plus US2 ownership before/after section identify command, facade, service, and version-specific ownership for changed paging, discovery, query strategy, progress, rendering, confirmation, and mutation planning paths. |

## Final Handoff Notes

- Final validation passed with `go test ./cmd ./c8volt/job ./c8volt/element ./c8volt/incident ./c8volt/process ./internal/services/ops -count=1`, `make docs-content`, `go test ./docsgen -count=1`, `make test`, and `git diff --check`.
- Full-suite validation required two test-support fixes: `c8volt/resource/client_test.go` now implements the current `process.API` stub surface, and `internal/services/ops/slow_process_analysis_test.go` uses `testx.SafeSlice` because explicit-key lookup is intentionally concurrent.
- Final changed source/test files are `c8volt/resource/client_test.go` and `internal/services/ops/slow_process_analysis_test.go`; final generated/artifact files are `docs/index.md`, `specs/254-cli-debt-refactor/assessment.md`, `specs/254-cli-debt-refactor/quickstart.md`, `specs/254-cli-debt-refactor/tasks.md`, `specs/254-cli-debt-refactor/ralph-memory.md`, and `specs/254-cli-debt-refactor/progress.md`.
